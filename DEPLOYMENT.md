# 🚀 Развертывание проекта на различных платформах

Инструкции по развертыванию StealthNET VPN на различных платформах и хостингах.

## 📋 Содержание

- [VPS сервер (рекомендуется)](#vps-сервер-рекомендуется)
- [Railway](#railway)
- [Fly.io](#flyio)
- [Render](#render)
- [DigitalOcean App Platform](#digitalocean-app-platform)
- [Heroku](#heroku)
- [DeployBot](#deploybot)

---

## 🖥️ VPS сервер (Рекомендуется)

**Лучший вариант для продакшена** - полный контроль, стабильность, низкая стоимость.

### Требования:
- VPS с Ubuntu 20.04+ / Debian 11+ / CentOS 8+
- Минимум 2GB RAM (рекомендуется 4GB+)
- 10GB+ свободного места
- Root доступ

### Установка:

Следуйте инструкции в **[INSTALLATION.md](./INSTALLATION.md)**

**Преимущества:**
- ✅ Полный контроль над сервером
- ✅ Низкая стоимость
- ✅ Стабильность
- ✅ Возможность масштабирования

**Провайдеры:**
- DigitalOcean
- Vultr
- Hetzner
- Linode
- AWS EC2
- Google Cloud Platform

---

## 🚂 Railway

Railway поддерживает Docker Compose и автоматическое развертывание из GitHub.

### Установка:

1. **Создайте аккаунт на [Railway](https://railway.app/)**

2. **Подключите GitHub репозиторий**

3. **Создайте новый проект** и выберите "Deploy from GitHub repo"

4. **Настройте переменные окружения** в разделе Variables:
   - Добавьте все переменные из `env.example`

5. **Railway автоматически обнаружит `docker-compose.yml`**

6. **Настройте домен** в разделе Settings → Domains

### Особенности:
- ✅ Автоматическое развертывание из GitHub
- ✅ Поддержка Docker Compose
- ✅ SSL сертификаты автоматически
- ⚠️ Может потребоваться настройка портов

**Документация:** https://docs.railway.app/

---

## ✈️ Fly.io

Fly.io поддерживает Docker и может развернуть ваш проект.

### Установка:

1. **Установите Fly CLI:**
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. **Войдите в аккаунт:**
   ```bash
   fly auth login
   ```

3. **Создайте приложение:**
   ```bash
   fly launch
   ```

4. **Настройте переменные окружения:**
   ```bash
   fly secrets set JWT_SECRET_KEY=your_key
   fly secrets set CLIENT_BOT_TOKEN=your_token
   # ... и т.д.
   ```

5. **Разверните:**
   ```bash
   fly deploy
   ```

**Документация:** https://fly.io/docs/

---

## 🎨 Render

Render поддерживает Docker и автоматическое развертывание.

### Установка:

1. **Создайте аккаунт на [Render](https://render.com/)**

2. **Подключите GitHub репозиторий**

3. **Создайте новый Web Service**

4. **Настройки:**
   - **Build Command:** `docker compose build`
   - **Start Command:** `docker compose up`
   - **Environment:** Docker

5. **Добавьте переменные окружения** в разделе Environment

6. **Разверните**

**Документация:** https://render.com/docs

---

## 🌊 DigitalOcean App Platform

DigitalOcean App Platform поддерживает Docker Compose.

### Установка:

1. **Создайте аккаунт на [DigitalOcean](https://www.digitalocean.com/)**

2. **Создайте новый App** → "GitHub" → выберите репозиторий

3. **Настройте:**
   - **Type:** Docker Compose
   - **Docker Compose File:** `docker-compose.yml`

4. **Добавьте переменные окружения**

5. **Разверните**

**Документация:** https://docs.digitalocean.com/products/app-platform/

---

## 🟣 Heroku

Heroku поддерживает Docker через Container Registry.

### Установка:

1. **Установите Heroku CLI:**
   ```bash
   # Ubuntu/Debian
   curl https://cli-assets.heroku.com/install.sh | sh
   ```

2. **Войдите:**
   ```bash
   heroku login
   ```

3. **Создайте приложение:**
   ```bash
   heroku create your-app-name
   ```

4. **Настройте переменные окружения:**
   ```bash
   heroku config:set JWT_SECRET_KEY=your_key
   heroku config:set CLIENT_BOT_TOKEN=your_token
   ```

5. **Разверните:**
   ```bash
   heroku container:push web
   heroku container:release web
   ```

**Примечание:** Heroku больше не предоставляет бесплатный план.

**Документация:** https://devcenter.heroku.com/articles/container-registry-and-runtime

---

## 🤖 DeployBot

DeployBot поддерживает развертывание с использованием Docker контейнеров.

### Установка:

1. **Создайте аккаунт на [DeployBot](https://deploybot.com/)**

2. **Подключите GitHub репозиторий**

3. **Создайте новый сервер** и настройте:
   - **Build:** Docker Compose
   - **Deploy:** Docker Compose

4. **Настройте переменные окружения**

5. **Настройте автоматическое развертывание**

**Документация:** https://deploybot.com/docs

---

## 🔧 Общие рекомендации

### Переменные окружения

На всех платформах необходимо настроить следующие переменные:

**Обязательные:**
- `JWT_SECRET_KEY`
- `API_URL`
- `ADMIN_TOKEN`
- `DEFAULT_SQUAD_ID`
- `CLIENT_BOT_TOKEN`
- `YOUR_SERVER_IP`
- `FERNET_KEY`

**Опциональные:**
- Настройки платежных систем
- Email настройки
- И другие из `env.example`

### Порты

Убедитесь, что платформа поддерживает:
- Порт 5000 для API
- Порт 80 для HTTP
- Порт 443 для HTTPS

### Volumes

Некоторые платформы могут не поддерживать volumes из `docker-compose.yml`. В этом случае:
- Используйте внешние хранилища (S3, Cloud Storage)
- Или используйте встроенные хранилища платформы

### База данных

Для продакшена рекомендуется использовать внешнюю БД:
- PostgreSQL (предпочтительно)
- MySQL
- Или использовать встроенные БД платформы

---

## 📝 Пример настройки для облачных платформ

### Railway / Render / Fly.io

Эти платформы обычно автоматически обнаруживают `docker-compose.yml`, но могут потребовать:

1. **Создание `railway.json` / `render.yaml` / `fly.toml`** (опционально)
2. **Настройка переменных окружения** через веб-интерфейс
3. **Настройка домена** через веб-интерфейс

### Рекомендация

Для продакшена **лучше всего использовать VPS сервер** с полным контролем, так как:
- Проект использует несколько сервисов (API, Bot, Nginx)
- Требуется постоянное хранение данных (БД, логи)
- Нужен полный контроль над конфигурацией

---

## 🆘 Поддержка

Если у вас возникли проблемы с развертыванием:
- Проверьте логи на платформе
- Убедитесь, что все переменные окружения настроены
- Проверьте документацию платформы
- См. [INSTALLATION.md](./INSTALLATION.md) для детальной инструкции
