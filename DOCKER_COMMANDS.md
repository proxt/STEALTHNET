# 🐳 Команды для работы с Docker

## 🔐 Вход в Docker контейнеры

### Войти в контейнер API (Flask)
```bash
docker exec -it stealthnet-api bash
```

### Войти в контейнер Bot (Telegram Bot)
```bash
docker exec -it stealthnet-bot bash
```

### Войти в контейнер Nginx
```bash
docker exec -it stealthnet-nginx sh
```
*Примечание: Nginx использует Alpine Linux, поэтому там `sh` вместо `bash`*

---

## 📋 Основные команды Docker

### Просмотр запущенных контейнеров
```bash
docker compose ps
# или
docker ps
```

### Просмотр всех контейнеров (включая остановленные)
```bash
docker ps -a
```

### Просмотр логов
```bash
# Логи всех сервисов
docker compose logs -f

# Логи конкретного сервиса
docker compose logs -f api
docker compose logs -f bot
docker compose logs -f nginx

# Последние 100 строк логов
docker compose logs --tail=100 api
```

### Остановка и запуск
```bash
# Остановить все контейнеры
docker compose stop

# Запустить все контейнеры
docker compose start

# Перезапустить конкретный контейнер
docker compose restart api

# Остановить и удалить контейнеры
docker compose down

# Запустить в фоновом режиме
docker compose up -d
```

### Пересборка образов
```bash
# Пересобрать все образы
docker compose build

# Пересобрать без кэша
docker compose build --no-cache

# Пересобрать и запустить
docker compose up -d --build
```

---

## 📊 Работа с базой данных в контейнере

### Найти базу данных
```bash
# Войти в контейнер API
docker exec -it stealthnet-api bash

# Внутри контейнера найти базу данных
find /app -name "*.db" -type f

# Или проверить текущую директорию
ls -la /app/
```

### Просмотр базы данных (если sqlite3 установлен)
```bash
# Войти в контейнер
docker exec -it stealthnet-api bash

# Использовать sqlite3
sqlite3 /app/stealthnet.db

# В SQLite консоли:
.tables          # Показать все таблицы
.schema users    # Показать структуру таблицы
SELECT * FROM users LIMIT 10;  # Пример запроса
.quit            # Выход
```

### Установить sqlite3 в контейнер (если нужно)
```bash
# Войти в контейнер
docker exec -it stealthnet-api bash

# Установить sqlite3
apt-get update
apt-get install -y sqlite3
```

---

## 📁 Работа с файлами в контейнере

### Просмотр файлов
```bash
# Войти в контейнер
docker exec -it stealthnet-api bash

# Просмотр структуры директорий
ls -la /app/
ls -la /app/instance/
ls -la /app/logs/
```

### Копирование файлов из контейнера
```bash
# Скопировать файл из контейнера на хост
docker cp stealthnet-api:/app/stealthnet.db ./backup.db

# Скопировать директорию
docker cp stealthnet-api:/app/logs ./logs_backup
```

### Копирование файлов в контейнер
```bash
# Скопировать файл с хоста в контейнер
docker cp ./file.txt stealthnet-api:/app/

# Скопировать директорию
docker cp ./logs stealthnet-api:/app/logs_backup
```

---

## 🔍 Полезные команды для диагностики

### Проверить переменные окружения в контейнере
```bash
docker exec stealthnet-api env | grep -E "SQLALCHEMY|JWT|FERNET"
```

### Проверить использование ресурсов
```bash
docker stats
```

### Проверить сеть
```bash
docker network ls
docker network inspect stealthnet-network
```

### Проверить volumes
```bash
docker volume ls
```

### Выполнить команду без входа в контейнер
```bash
# Выполнить команду в контейнере
docker exec stealthnet-api python3 --version
docker exec stealthnet-api ls -la /app/
docker exec stealthnet-api cat /app/.env
```

---

## 🛠️ Отладка

### Проверить, что контейнер запущен
```bash
docker compose ps
```

### Проверить логи на ошибки
```bash
docker compose logs api | grep -i error
docker compose logs bot | grep -i error
```

### Перезапустить контейнер с очисткой
```bash
# Остановить и удалить контейнеры
docker compose down

# Удалить volumes (ОСТОРОЖНО! Удалит данные)
docker compose down -v

# Пересобрать и запустить
docker compose up -d --build
```

---

## 📝 Примеры использования

### Войти в API контейнер и проверить базу данных
```bash
docker exec -it stealthnet-api bash
cd /app
ls -la *.db
sqlite3 stealthnet.db ".tables"
```

### Скопировать логи на хост
```bash
docker cp stealthnet-api:/app/logs ./logs_backup
```

### Проверить конфигурацию
```bash
docker exec stealthnet-api cat /app/.env
docker exec stealthnet-nginx cat /etc/nginx/conf.d/default.conf
```

---

## ⚠️ Важно

1. **Не удаляйте volumes** без необходимости - там хранятся данные
2. **Делайте бэкапы** перед важными операциями
3. **Проверяйте логи** при возникновении проблем
4. **Используйте `docker compose`** вместо `docker-compose` (новая версия)

